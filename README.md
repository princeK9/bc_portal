# Archive branch — original pre-refactor code

This branch preserves the codebase exactly as it existed before the engineering work documented on `main` began. Kept for direct comparison, not for use — see `main` for the current, working version.
