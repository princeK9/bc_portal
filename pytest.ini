[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --strict-markers
# caplog assertions in test_pipeline rely on records propagating to the root
# logger, which is where config.setup_logging() attaches its handler.
log_cli_level = INFO
